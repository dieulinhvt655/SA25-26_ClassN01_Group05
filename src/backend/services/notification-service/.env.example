# Cấu hình môi trường cho Notification Service

# Server Configuration
PORT=3005
NODE_ENV=development

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=notification_service_db
DB_USER=root
DB_PASSWORD=

# RabbitMQ Configuration
RABBITMQ_URL=amqp://localhost:5672
RABBITMQ_EXCHANGE=notification.exchange
RABBITMQ_QUEUE=notification.queue

# Firebase Configuration (simulation)
FIREBASE_ENABLED=false

# Email Configuration (simulation)
EMAIL_ENABLED=false
