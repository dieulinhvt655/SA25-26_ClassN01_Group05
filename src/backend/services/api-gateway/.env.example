# API Gateway Configuration
GATEWAY_PORT=3000
GATEWAY_HOST=localhost

# Microservices URLs (phải trùng với PORT mỗi service khi chạy)
CART_SERVICE_URL=http://localhost:3002
ORDER_SERVICE_URL=http://localhost:3003
RESTAURANT_SERVICE_URL=http://localhost:3004
NOTIFICATION_SERVICE_URL=http://localhost:3005
DISCOVERY_SERVICE_URL=http://localhost:3006
USER_SERVICE_URL=http://localhost:3007

# Lưu ý: User-service mặc định chạy port 3002 (trùng Cart). Khi chạy cả hai, set PORT=3007 cho user-service.
# Discovery-service mặc định 3003 (trùng Order). Khi chạy cả hai, set PORT=3006 cho discovery-service.

# Environment
NODE_ENV=development
