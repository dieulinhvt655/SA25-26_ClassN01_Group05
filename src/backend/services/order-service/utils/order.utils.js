/**
 * Utility cho Order – nghiệp vụ đã được xoá.
 */

function calculateItemTotal(/* unitPrice, quantity */) {
    return 0;
}

function calculateOrderTotal(/* items */) {
    return 0;
}

module.exports = {
    calculateItemTotal,
    calculateOrderTotal
};
